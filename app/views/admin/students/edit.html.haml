
.row
  .col-md-5
    %h1= student.display_name

    = simple_form_for([:admin, student], html: { class: 'form-horizontal' }) do |f|

      = f.input :first_name
      = f.input :last_name
      = f.input :email
      = f.input :card_code

      = f.button :submit
