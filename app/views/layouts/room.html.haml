!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %meta{:charset => "utf-8"}/
    %meta{:content => "IE=edge", "http-equiv" => "X-UA-Compatible"}/
    %meta{:content => "width=device-width, initial-scale=1", :name => "viewport"}/
    %title Glims
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  %body.room

    #header
      = link_to 'Swing City', room_path

      - if content_for?(:header)
        = yield :header

      - if @course_log
        %span= @course_log.date.to_human
        %span= @course_log.course.calendar_name
        %span= @course_log.teachers.map(&:name).join(" - ")
        %span.flex-right
          = link_to 'Cerrar', room_path

    #container
      = yield

    - if content_for?(:footer)
      #footer
        %hr
        = yield :footer
