= simple_form_for(student, url: cashier_student_path(place, student), html: { class: 'form-horizontal' }) do |f|

  .col-md-5
    %h1= student.display_name

    = f.input :first_name
    = f.input :last_name
    = f.input :email

    = f.input :card_code
    %i
      Si cambia la tarjeta recuerde cobrar los
      = "$#{FixedFee.new_card_fee}"
      por la misma.

    %br
    %br

    = f.button :submit
  .col-md-1
  .col-md-5
    %br
    %br
    %br
    %br

    %blockquote
      = f.input :comment
      %footer
        = "#{student.comment_by.try(&:name)}, "
        = student.comment_at.try(&:to_human)


