.row
  .col-md-4
    %h4 Pago por alquiler de sala

    = simple_form_for(TeacherCashIncomes::VenueRent.new, url: teacher_venue_rent_path, html: { class: 'form-horizontal' }) do |f|
      = hidden_field_tag :next_url, next_url
      = f.input :payment_amount
      = f.input :description
      = f.button :submit
